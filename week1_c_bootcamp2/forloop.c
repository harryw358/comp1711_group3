#include <stdio.h>

int main() {

    int a;

    for (a = 0; a < 5; a++) {
        printf("a is equal to %d\n", a);
    }

    printf("After the for loop finished a is equal to %d", a);
    return 0;
}